{
	"mcpServers": {
		"svelte": {
			"command": "npx",
			"args": ["-y", "@sveltejs/mcp"]
		},
		"mcp_docker": {
			"command": "docker",
			"args": ["mcp", "gateway", "run"],
			"env": {
				"DOCKER_CONTEXT": "remote"
			}
		}
	}
}
